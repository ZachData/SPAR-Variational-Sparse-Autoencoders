# The project is a package now: `pip install -e .` for the framework and
# reproduce.py (numpy/scipy/matplotlib), `pip install -e ".[train]"` to
# train, `".[e4]"` for the SAEBench size control. requirements-lock.txt is
# the exact environment every result was produced in.
-e .[train,e4,dev]
